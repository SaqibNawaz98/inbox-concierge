/** Shown when learning APIs are unavailable (no DATABASE_URL or DB unreachable). */
export const LEARNING_OPTIONAL_SETUP_MESSAGE =
  "Learning is off: this server has no DATABASE_URL, or Postgres is not reachable.";
